# bc-site Active Problems

## How To Use This File
- Keep only active or near-term frontend items here.
- Link to `specs/*.md` when the work needs more than a short note.
- Track only `Draft`, `Ready`, and `In Progress` work here by default.
- Move stable completed work to `archive/` when the history should be preserved.

## Template
- [ ] Title
  - Status: Draft | Ready | In Progress
  - Spec:
  - Expected behavior:
  - Acceptance signal:
  - Related code:
  - Notes:

## Open
- [ ] SDD baseline
  - Status: Ready
  - Spec: `specs/sdd-maintenance.md`
  - Expected behavior: frontend specs live under `bc-site/sdd/`, with cross-repo API changes linked to backend or workspace specs.
  - Acceptance signal: project SDD structure exists and frontend-only work no longer depends on workspace-level `sdd/`.
  - Related code: `bc-site/AGENT.md`
  - Notes: keep frontend API fields in `camelCase`.

## In Progress

## Recently Done
