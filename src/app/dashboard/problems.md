# 整体问题

- 其实现在menu所有页面点进去都是一个列表页，然后列表页可能会有增改的便捷页面，同时也会有额外的其他页面，但是列表页应该都是沿用同一个component
- tab 页面默认都需要可关闭的标签,关闭tab后自动跳转到上一个使用的tab

## 列表页

- 现在的列表页的title 描述太长了，应该剪短，一个图表加标题即可，同行的右侧放置一些整体操作按钮（如果有的话），下面是filter,在下面是展示列表，可以是card可以是table,现在的整体布局过于松散，希望整个列表页面紧凑些,filter的间距也有点过大
